<tr id="project_{{ project.id }}" class="btn-row">
    <td><a href="{{ path('app_project_edit', {'id': project.id}) }}">{{ project.name }}</a></td>
    <td>{{ project.id }}</td>
    <td class="text-end btn-cell">
        
        <div class="btn-group btn-group-sm">
            <a href="{{ path('app_project_edit', {'id': project.id}) }}" class="btn btn-sm btn-warning">
                <i class="fa fa-pencil">&nbsp;</i>
            </a>
            {{ include('admin/project/_delete_form.html.twig') }}
        </div>

    </td>
</tr>
